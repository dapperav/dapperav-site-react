---
import { Image } from 'astro:assets';
import logo from '../assets/logo.png';
import { site } from '../data/site';

const links = [
  { href: '/', label: 'Home' },
  { href: '/services', label: 'Services' },
  { href: '/work', label: 'Our Work' },
];

const path = Astro.url.pathname.replace(/\/$/, '') || '/';
---

<header
  id="site-nav"
  class="fixed inset-x-0 top-0 z-50 border-b border-tan/40 bg-cream/90 backdrop-blur-md transition-all duration-300"
>
  <div
    class="mx-auto flex max-w-6xl items-center justify-between px-5 py-4 transition-all duration-300 [#site-nav.is-condensed_&]:py-2 sm:px-8"
  >
    <a href="/" class="flex items-center gap-3" aria-label="Dapper AV Solutions — home">
      <Image src={logo} alt="" width="96" height="96" class="h-12 w-12 object-contain" loading="eager" />
      <span class="font-display text-lg leading-tight font-semibold tracking-wide">
        Dapper AV
        <span class="block text-[0.65rem] font-normal tracking-[0.28em] text-cocoa uppercase">Solutions</span>
      </span>
    </a>

    <nav class="hidden items-center gap-8 md:flex" aria-label="Main">
      {
        links.map((l) => (
          <a
            href={l.href}
            aria-current={path === l.href ? 'page' : undefined}
            class="link-draw text-sm font-medium tracking-wide text-espresso"
          >
            {l.label}
          </a>
        ))
      }
      <a href={site.phoneHref} class="link-draw text-sm font-medium tracking-wide text-cocoa">{site.phone}</a>
      <a
        href="/quote"
        class="btn-sheen rounded-sm bg-gold px-5 py-2.5 text-sm font-semibold tracking-wide text-espresso shadow-sm transition-colors duration-300 hover:bg-gold-deep"
      >
        Request a Quote
      </a>
    </nav>

    <button
      id="nav-toggle"
      class="flex h-11 w-11 items-center justify-center md:hidden"
      aria-expanded="false"
      aria-controls="mobile-menu"
      aria-label="Open menu"
    >
      <span class="relative block h-4 w-6">
        <span
          class="absolute top-0 left-0 h-0.5 w-6 bg-espresso transition-all duration-300 [#nav-toggle[aria-expanded='true']_&]:top-1/2 [#nav-toggle[aria-expanded='true']_&]:-translate-y-1/2 [#nav-toggle[aria-expanded='true']_&]:rotate-45"
        ></span>
        <span
          class="absolute top-1/2 left-0 h-0.5 w-6 -translate-y-1/2 bg-espresso transition-all duration-300 [#nav-toggle[aria-expanded='true']_&]:opacity-0"
        ></span>
        <span
          class="absolute bottom-0 left-0 h-0.5 w-6 bg-espresso transition-all duration-300 [#nav-toggle[aria-expanded='true']_&]:top-1/2 [#nav-toggle[aria-expanded='true']_&]:-translate-y-1/2 [#nav-toggle[aria-expanded='true']_&]:-rotate-45"
        ></span>
      </span>
    </button>
  </div>

  <div
    id="mobile-menu"
    class="hidden border-t border-tan/40 bg-cream px-5 pt-2 pb-6 md:hidden"
  >
    <nav class="flex flex-col" aria-label="Mobile">
      {
        links.map((l) => (
          <a
            href={l.href}
            aria-current={path === l.href ? 'page' : undefined}
            class="border-b border-tan/30 py-4 font-display text-2xl aria-[current=page]:text-gold-deep"
          >
            {l.label}
          </a>
        ))
      }
      <a href={site.phoneHref} class="border-b border-tan/30 py-4 text-lg text-cocoa">Call {site.phone}</a>
      <a
        href="/quote"
        class="mt-5 rounded-sm bg-gold px-5 py-3.5 text-center text-base font-semibold text-espresso"
      >
        Request a Quote
      </a>
    </nav>
  </div>
</header>

<script>
  const nav = document.getElementById('site-nav')!;
  const toggle = document.getElementById('nav-toggle')!;
  const menu = document.getElementById('mobile-menu')!;

  let ticking = false;
  addEventListener(
    'scroll',
    () => {
      if (ticking) return;
      ticking = true;
      requestAnimationFrame(() => {
        nav.classList.toggle('is-condensed', scrollY > 24);
        nav.classList.toggle('shadow-md', scrollY > 24);
        ticking = false;
      });
    },
    { passive: true }
  );

  toggle.addEventListener('click', () => {
    const open = toggle.getAttribute('aria-expanded') === 'true';
    toggle.setAttribute('aria-expanded', String(!open));
    toggle.setAttribute('aria-label', open ? 'Open menu' : 'Close menu');
    menu.classList.toggle('hidden', open);
  });
</script>
